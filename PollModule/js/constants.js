const STORAGE_KEYS = Object.freeze({
    POLLS: "admin_polls"
});

const POLL_TYPES = Object.freeze({
    SINGLE: "single",
    MULTIPLE: "multiple"
});

const POLL_STATUS = Object.freeze({
    ACTIVE: "active",
    INACTIVE: "inactive"
});

const MIN_OPTIONS = 2;